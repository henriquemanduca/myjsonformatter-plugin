name: Release JSON Toolkit

on:
    push:
        tags:
            - "*"

jobs:
    release:
        runs-on: ubuntu-latest
        permissions:
            contents: write
            id-token: write
            attestations: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "22"
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Run quality checks and tests
              run: npm run check

            - name: Extract metadata and validate tag
              id: meta
              shell: bash
              run: |
                  TAG="${GITHUB_REF#refs/tags/}"
                  VERSION="$(node -p "JSON.parse(require('fs').readFileSync('./manifest.json', 'utf8')).version")"
                  PLUGIN_ID="$(node -p "JSON.parse(require('fs').readFileSync('./manifest.json', 'utf8')).id")"
                  
                  echo "Tag: $TAG"
                  echo "Manifest version: $VERSION"
                  echo "Plugin ID: $PLUGIN_ID"
                  
                  if [ "$TAG" != "$VERSION" ]; then
                    echo "::error::Tag ($TAG) does not match manifest.json version ($VERSION)"
                    exit 1
                  fi
                  
                  echo "tag=$TAG" >> "$GITHUB_OUTPUT"
                  echo "version=$VERSION" >> "$GITHUB_OUTPUT"
                  echo "plugin_id=$PLUGIN_ID" >> "$GITHUB_OUTPUT"

            - name: Inspect dist output
              shell: bash
              run: |
                  echo "Contents of dist/:"
                  ls -la dist || true

            - name: Verify release assets
              shell: bash
              run: |
                  test -f dist/main.js || (echo "::error::dist/main.js not found" && exit 1)
                  test -f dist/manifest.json || (echo "::error::dist/manifest.json not found" && exit 1)
                  
                  if [ -f styles.css ] && [ ! -f dist/styles.css ]; then
                    echo "::error::styles.css exists in repo but dist/styles.css was not generated"
                    exit 1
                  fi

            - name: Create zip package
              shell: bash
              run: |
                  PLUGIN_ID="${{ steps.meta.outputs.plugin_id }}"
                  TAG="${{ steps.meta.outputs.tag }}"
                  mkdir -p release-package
                  cp dist/main.js release-package/main.js
                  cp dist/manifest.json release-package/manifest.json
                  
                  if [ -f dist/styles.css ]; then
                    cp dist/styles.css release-package/styles.css
                  fi
                  
                  cd release-package
                  zip -r "../${PLUGIN_ID}-${TAG}.zip" .

            - name: Generate artifact attestations
              uses: actions/attest@v4
              with:
                  subject-path: |
                      dist/main.js
                      dist/manifest.json
                      dist/styles.css
                      ${{ steps.meta.outputs.plugin_id }}-${{ steps.meta.outputs.tag }}.zip

            - name: Create GitHub release
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              shell: bash
              run: |
                  PLUGIN_ID="${{ steps.meta.outputs.plugin_id }}"
                  TAG="${{ steps.meta.outputs.tag }}"
                  
                  ASSETS=(
                    "dist/main.js#main.js"
                    "dist/manifest.json#manifest.json"
                    "${PLUGIN_ID}-${TAG}.zip#${PLUGIN_ID}-${TAG}.zip"
                  )
                  
                  if [ -f dist/styles.css ]; then
                    ASSETS+=("dist/styles.css#styles.css")
                  fi
                  
                  gh release create "$TAG" \
                    --title="$TAG" \
                    --draft \
                    --generate-notes \
                    "${ASSETS[@]}"
